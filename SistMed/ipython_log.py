#log# Automatic Logger file. *** THIS MUST BE THE FIRST LINE ***
#log# DO NOT CHANGE THIS LINE OR THE TWO BELOW
#log# opts = Struct({'__allownew': True, 'logfile': 'ipython_log.py'})
#log# args = []
#log# It is safe to make manual edits below here.
#log#-----------------------------------------------------------------------
from django.contrib.auth.models import User
u = User.objects.get(username__exact='test')
u.groups
u.groups
str(u.groups)
u.
u.groups.
u.groups[0].
u.groups[0]
u.groups.
u.groups.values 
u.groups.values()
u.groups.values()['name']
u.groups.values()
u.groups.values()[0]['name']
u.groups.add('Pacientes')
u.groups.add().__doc__()
u.groups.add().__doc__
u.groups.add.__doc__()
u.groups.add.__doc__
from django.contrib.auth.models import User, Groups
from django.contrib.auth.models import User, Group
g = Group.objects.get(name__exact='Medicos')
g = Group.objects.get()
g = Group.objects.get.all()
g = Group.objects.
g = Group.objects.get(
g = Group.objects.get('Medicos')
g
print(g)
g = Group.objects.get(
g = Group.objects.get('Medicos')
;
g = Group.objects.get('Medicos')
